# Agent Documentation

This file contains information for coding agents working on this project.

## Deployment

See `./DEPLOYMENT.md` for deployment status, logs, troubleshooting, pipeline setup, and next steps.

The application is deployed to AWS using CloudFront + S3 static hosting. The deployment infrastructure is managed by AWS CDK.

Quick deployment command: `./scripts/deploy.sh`
